name: Daily Update

on:
  schedule:
    - cron: '55 0 */2 * *' # 每2天凌晨00点55分执行任务（上海时区对应的UTC时间）
  workflow_dispatch:
    branches:
      - main

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: pip install requests

    - name: Run main script
      run: python main.py

    - name: Download EPG File (新增步骤)
      run: wget http://epg.51zmt.top:8000/e.xml -O epg.xml
      # 从指定 URL 下载 EPG 文件并保存为 epg.xml（需确保 URL 有效）

    - name: Configure Git user
      run: |
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"

    - name: Add changed files (包含 epg.xml)
      run: git add live_ipv4.m3u live_ipv6.m3u live_ipv4.txt live_ipv6.txt epg.xml

    - name: Commit and push if changes exist
      run: |
        if git diff --staged --exit-code; then
          echo "No changes to commit"
        else
          git commit -m "Update live streams and EPG"
          git push
        fi
    env:
      TZ: Asia/Shanghai
